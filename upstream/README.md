# Optional upstream checkout

Run:

```bash
npm run upstream:init
```

to clone `https://github.com/cathrynlavery/diagram-design.git` into
`upstream/diagram-design`.

This directory is ignored by Git because the repository can also be managed as
a submodule.
